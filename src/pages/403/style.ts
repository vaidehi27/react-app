import styled from 'styled-components';
const error = require('../../assets/error.svg') as string;

export const ErrorIcon = styled.div`
  background: url(${error});
  width: 180px;
  height: 180px;
`;
